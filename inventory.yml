all:
  children:
    CVP_SERVERS:
      hosts:
        cvaas:
          # Staging CVaaS
          ansible_host: www.cv-staging.corp.arista.io
          # Prod CVaaS
          #ansible_host: www.arista.io